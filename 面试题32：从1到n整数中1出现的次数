书中的解法不太容易理解，以下博客给出的解法浅显易懂
http://blog.csdn.net/sjf0115/article/details/8600599
设N=abcde，则1出现的次数等于个位数1出现的次数+十位数1出现的次数+。。。+万位数1出现的次数
每一位上分为三种情况
1、若此位上数字为0，则此位上可能出现1的次数由更高位决定
2、若此位上数字为1，则此位上可能出现1的次数不仅受高位影响还受低位影响
3、若此位上数字大于1，则此位上可能出现1的次数仅由高位决定
/*
N=abcde，百位上数字是c，仅求百位上出现1的情况为例
*/
int count=0;

if(c == 0)
  //等于更高位数字（ab）×当前位数（100）
  count += ab*100;
else if (c == 1)
  //等于更高位数字（ab）×当前位数+低位数字（de）+1
  count += ab*100+de+1;
else
  //（更高位数字+1）×当前位数
  count += (ab+1)*100
